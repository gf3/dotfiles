;;; init-direnv.el --- Mode line configuration. -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package direnv
  :straight t
  :config
  (direnv-mode))

(provide 'init-direnv)
;;; init-direnv.el ends here
